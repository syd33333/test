    @Bean
    public MessageChannel outputChannel2() {
        return MessageChannels.direct().get(); // Second output channel (e.g., for "channel2")
    }

    @Bean
    public MessageChannel nullChannel() {
        return MessageChannels.nullChannel().get(); // Discards messages that don’t need further processing
    }
